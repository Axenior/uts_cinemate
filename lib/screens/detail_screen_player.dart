import 'package:flutter/material.dart';

class DetailScreenPlayer extends StatelessWidget {
  const DetailScreenPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

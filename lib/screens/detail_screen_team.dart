import 'package:flutter/material.dart';

class DetailScreenTeam extends StatelessWidget {
  const DetailScreenTeam({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
